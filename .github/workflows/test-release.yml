name: Test Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from test workflow with permissions"
